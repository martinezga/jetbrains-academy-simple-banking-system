import random


random.seed(3)
# call the function here
print(random.betavariate(0.9, 0.1))

"""
Beta distribution
Python is widely used for mathematical purposes. 
Let's start gaining experience by using the betavariate(alpha, beta) function from the random module.
Check the random module's documentation to find out more about the function in question.

Generate a number from the beta distribution with parameters alpha=0.9 and beta=0.1. Print your result.
"""